fpath+=("$(brew --prefix)/share/zsh/site-functions")

autoload -U promptinit; promptinit
prompt pure

source $PWD/.dotfiles/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh

source $PWD/.dotfiles/.aliases
